async function validateCommand(apiUrl, _options = {}) {
  // TODO: Validate real API against contract
  console.log(`Validating API at ${apiUrl} against contract...`);
}

export default validateCommand;